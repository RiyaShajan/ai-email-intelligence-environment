name: ai-email-intelligence
description: AI agent environment for email classification and response generation

tasks:
  - id: easy
    description: classify email type
    max_steps: 5

  - id: medium
    description: generate reply to email
    max_steps: 10

  - id: hard
    description: prioritize and respond to multiple emails
    max_steps: 15

entrypoint: inference.py
